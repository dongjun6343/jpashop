spring:
#  datasource:
    # 메모리 모드.(테스트 시)
    # 스프링부트는 기본세팅이 없으면 메모리 모드로 실행시킨다.
#    url:  jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    # org.hibernate.type: trace : parameter값을 로그 찍어줌.
    org.hibernate.type: trace